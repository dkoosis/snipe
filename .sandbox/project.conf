# .sandbox/project.conf — snipe
PROJECT_NAME=snipe

# go-sandbox version (tag, branch, or commit hash; "latest" = HEAD)
GO_SANDBOX_REF=v0.2.0

# Binaries to cross-compile (space-separated "name:path" pairs)
# snipe builds from the module root.
PROJECT_BINS="snipe:."

# Tools provided by the Codex base image (assumed present, never cross-compiled)
BASE_IMAGE_TOOLS="go jq rg fd"

# Tools we provide via prebuilt binaries
# Note: snipe is the project itself — listed in PROJECT_BINS, not here.
PREBUILT_TOOLS="golangci-lint govulncheck gofumpt goimports fo bat dtree"

# OPTIONAL_TOOLS: nice-to-have, not fatal if missing
OPTIONAL_TOOLS=""

# go mod download needs local-replace stripping (snipe has no local replaces)
HAS_LOCAL_REPLACES=false

# Extra env vars for setup (space-separated KEY=VALUE)
SETUP_ENV=""

# Build system (make or mage)
BUILD_SYSTEM=make

# Single fleet pin for golangci-lint. CI and the sandbox both read this key —
# there is exactly one place this version is written (conform lint-pin rule).
GOLANGCI_LINT_VERSION=v2.12.2

# fo renders test output (make test/race pipe through it); CI installs this
# pinned version so make check runs identically on runner and dev machine.
FO_VERSION=v0.2.3

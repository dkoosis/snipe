# Boot
updated: 2026-05-05

→ next: `bd ready` and pick — snipe-fac complexity rollup (last snipe-96h child); or snipe-8xk surface high-D / LCOM4 in `snipe context`; or lifecycle bug cluster (dhq/0sb/13u/29j/84h)

✓ done
- snipe-gq4: LCOM4 cohesion (`snipe metrics --kind=lcom4`); per-pkg connected components of intra-pkg call graph (production funcs/methods, no _test.go, no .test pkgs); union-find in Go; LCOM4 > 1 = split candidate. Self-scan: cmd=41, internal/query=33, internal/output=21 = top split candidates.
- snipe-g63: distance-from-main-sequence (`snipe metrics --kind=distance`); D = |A + I − 1|, joins abstractness + instability at query time, sorts desc, ! flags D > 0.70. Follow-up snipe-8xk for context-surface integration.
- snipe-cat: abstractness metric (`snipe metrics --kind=abstractness`); A = interface / (iface+struct+type), exported, production-only

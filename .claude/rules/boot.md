# Boot
updated: 2026-05-06

→ next: `bd ready` and pick. Eval all-PASS; remaining `bbolt-cross-01` Tx.Commit gap needs transitive `callers` (design work, brainstorm before coding).

✓ done
- snipe-lwk: T.Method form resolves for unexported types (`node.put`). Closed bbolt-search-02.
- snipe-3js: callgraph interface-dispatch via `types.Implements` (cross-pkg DI). Callers 100%.

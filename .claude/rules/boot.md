# Boot

## lane: TameCaribou
updated: 2026-07-06

→ next: sn-tgra — calibrate `snipe risk` thresholds/weights. First locate or rebuild the ccp-1lk5 20-PR replay harness (not found in cc-plugins; snipe eval siblings `../cobra ../fzf` absent). Full signal set now present to calibrate against.

✓ done
- sn-aa2 bead-DAG centrality signal shipped → `snipe risk` (#188, merged cda9a59). Epic sn-06o 3/4.

‡ traps: `docs/eval/task_status.txt` drifts on every `make audit` — don't stage it.

~ rapport: dk steers fork-by-fork, merges decisively once the gate is green.

# Boot
updated: 2026-04-24

→ zhs.3 shipped. Next: zhs.4 (M3 deps×refs boundary) — requires_plan + requires_review.

1. `bd show snipe-zhs.4`
2. Draft design in bead `--design` field
3. Invoke /writing-plans, then dispatch

state: main @ 3d93800, `make audit` green, zhs epic 3/4

## done this session
- zhs.3: `snipe lits <value>` — indexes string literals + env var call sites (migration 14)

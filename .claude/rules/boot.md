# Boot
updated: 2026-06-15 (76d9975)

→ next: `bd ready` and pick one. snipe-p61 (testscript suite) still `human`-flagged — wait for dk. ‡ eval siblings need reindex before score comparisons (.test pkg removal changed symbol counts).

✓ done
- `snipe guard` shipped (#162): reference-graph boundary gate, fail-closed exits. Post-review fix: allow-list suffix now path-aligned (`db.go` ≠ `validb.go`).

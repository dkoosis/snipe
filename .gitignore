# Binaries (local build output — NOT .bin/ prebuilts)
/snipe
/bin/
/dist/

# Prebuilt sandbox binaries — tracked in .bin/linux-{amd64,arm64}/
# (do NOT add .bin/ here)

# Index database (local, regenerated by snipe index)
.snipe/

# Sandbox caches + generated reports
.sandbox/cache/
.sandbox/setup-report.json
.sandbox/bin/**/*.tmp
.sandbox/bin/**/*.upx
.codex/cache/
.claude/cache/

# Worktrees
.claude/worktrees/

# IDE
.idea/
.vscode/
*.swp

# macOS
.DS_Store

# Test artifacts
coverage.out

# Eval benchmark repos and results
.eval-repos/
docs/eval/EVAL_RESULTS.json
.worktrees/

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key

# Local tool output
.claude/tmp/
.fo/
.claude/scheduled_tasks.lock
bin/
# bd worktree
sn-nduv/
# bd worktree
sn-8f6q.5/
# bd worktree
sn-8f6q.6/
# bd worktree
sn-8f6q.4/
# bd worktree
snipe-p61/
# bd worktree
sn-5xen/
# bd worktree
sn-8f6q.9/
# bd worktree
sn-8f6q.8/
# bd worktree
sn-zjwh/
# bd worktree
sn-t0ge/
# bd worktree
sn-l1kh.7/
# bd worktree
sn-l1kh.8/
# bd worktree
sn-yhl4/

# bd audit residue: passive re-export, Dolt is source of truth
.beads/interactions.jsonl
